#include "TSP.h"
#include <ostream>

int main(int argc, char** argv){
	TSP *TSPGraph = new TSP(argv[0]);
	for (float* : TSP::list){
		for (float j : i){
			std::cout << i << ' ';
		}
		std::cout << '/n';
	}
}
