#include <TSP.hpp>

int main(int argc, char** argv){
	TSP *TSPGraph = new TSP(argv[0]);
	for (double* : TSP::list){
		for (double j : i){
			std::cout << i << ' ';
		}
		std::cout << '/n';
	}
}
